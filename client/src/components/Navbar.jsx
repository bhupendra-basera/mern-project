import React from 'react';

const Navbar = () => {
    return (
        <nav style={{ background: '#f0f0f0', padding: '10px'}}>
            <h1> Donation App Nav (TODO)</h1>
            {/* Add navigation links here later*/}
        </nav>
    );
};

export default Navbar;