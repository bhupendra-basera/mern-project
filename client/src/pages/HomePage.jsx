import React from 'react';

const HomePage = () => {
    return (
        <div>
            <h2>Welcome to the Donation Platform!</h2>
            {/* Add links to other pages using react-router-dom Link component later */}
        </div>
    );
};

export default HomePage;